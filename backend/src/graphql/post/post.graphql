extend type Query {
  post(id: ID!): Post
  posts: [Post]!
}

extend type Mutation {
  createPost(userId: ID!, title: String!, content: String!): Post
}

type Post {
  id: ID!
  title: String!
  content: String!
  author: User!
}
