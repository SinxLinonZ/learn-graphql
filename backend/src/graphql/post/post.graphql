extend type Query {
  getPost(id: ID!): Post
  listPosts: [Post]!
}

extend type Mutation {
  createPost(userId: ID!, title: String!, content: String!): Post
}

type Post {
  id: ID!
  authorId: ID!
  title: String!
  content: String!
}
